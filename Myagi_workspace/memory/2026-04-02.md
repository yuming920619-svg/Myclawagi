# 2026-04-02

## 升級前 GitHub 備份
- source: chat + git
- tags: #workspace #github #backup #upgrade
- note: 在評估 OpenClaw `2026.3.24 -> 2026.4.1` 升級前，已先將目前工作區快照同步到 GitHub repo `yuming920619-svg/Myclawagi`：commit `b0c43ab`（`chore: backup workspace snapshot before OpenClaw upgrade`）。本次同步包含 `Myagi_workspace/` 的最新記憶/設定更新，以及 `Expense tracking/2026-04.csv`。

## OpenClaw 已升級至 2026.4.1
- source: chat + session_status
- tags: #openclaw #upgrade #verification
- note: 使用者通知已完成升級；本 session 的 `session_status` 已顯示 OpenClaw `2026.4.1`，確認升級生效。

## Telegram default bot 開啟 exec approvals
- source: chat + config edit
- tags: #telegram #exec #approvals #config
- note: 已在 `~/.openclaw/openclaw.json` 的 `channels.telegram.accounts.default` 啟用 `execApprovals`，approver 設為使用者 Telegram ID `8087282597`、target=`dm`，並把 `capabilities.inlineButtons` 設為 `dm`。目前只對 `default` bot 啟用，未動 `reminder` 與 `8562448721` 兩個 account；需重啟 gateway 後生效。

## Telegram exec approval 測試成功
- source: chat + exec
- tags: #telegram #exec #approvals #verification
- note: 重啟 gateway 後，以 `ask=always` 送出 `openclaw --version && echo '---' && openclaw status` 測試；default bot DM 成功收到 approval prompt，使用者 allow-once 後命令正常執行完成，表示 Telegram exec approvals 路徑已生效。

## 升級後待留意的 security warnings
- source: `openclaw status`
- tags: #openclaw #security #warnings #post-upgrade
- note: 升級到 `2026.4.1` 後，狀態檢查顯示幾項值得後續 hardening 的警告：`gateway.controlUi.allowInsecureAuth=true`、`gateway.trustedProxies` 未設、remote CDP 目前使用 HTTP、以及 `gateway.nodes.denyCommands` 有些規則實際上不生效。這些目前不是升級失敗，但應視為後續安全設定待辦。

## 記帳 approval 降噪修正
- source: chat + exec approvals + skill edit
- tags: #expense #telegram #exec #approvals #workflow
- note: 為減少 Telegram exec approval 干擾，已把記帳流程中常用的三個固定腳本都走 allow-always：`skills/expense-tracker/scripts/stats.sh`、`expenses/sync-to-github.sh`、`skills/expense-tracker/scripts/send-reminder.sh`。其中 reminder 發送已新增固定 wrapper `send-reminder.sh`（內部呼叫 `send-reminder.js` 從月檔自動計算當月總額/筆數/分類摘要），取代每次手組動態 `openclaw message send`。若之後只是一般單筆記帳，正常情況下應不再需要額外批准；除非流程再變更、改腳本內容，或臨時跑新的 host exec 指令。
