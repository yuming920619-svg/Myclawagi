# 2026-04-03

## 記帳免批准流程實測成功
- source: chat + expense csv + reminder send
- tags: #expense #telegram #exec #approvals #verification
- note: 4/3 午餐「拉亞漢堡 206」已用新的固定流程完成：寫入 `expenses/2026-04.csv`、透過 `skills/expense-tracker/scripts/send-reminder.sh` 成功送出 reminder bot，且整體過程未再出現新的 approval prompt，表示 4/2 做的記帳 approval 降噪修正（allow-always + wrapper 腳本）已在真實單筆記帳上驗證成功。

## 待辦事項網頁專案：GitHub 內容不完整，暫時無法直接部署
- source: chat + github api/web fetch
- tags: #todo #github #deployment #audit
- note: 檢查 `yuming920619-svg/Myclawagi` 的 `待辦事項/` 後，確認目前只有 Next.js 骨架根目錄檔案，缺少 README 提到的 `app/`、`lib/`、`supabase/`、`docs/`、`tests/` 等關鍵內容。`middleware.ts` 匯入了不存在的 `@/lib/env`，`package.json` 也引用不存在的 `tests/task-schemas.test.ts`，因此目前不是單純少部署，而是 repo 尚未完整 push，需先補齊原始碼或由我接手重建後才能繼續部署。

## 待辦事項專案 zip 未成功傳入
- source: chat + exec + visible attachment
- tags: #todo #telegram #attachment #blocker
- note: 使用者嘗試直接上傳完整專案 zip，但檢查 `/home/node/.openclaw/media/inbound` 後，沒有出現 2026-04-03 的新 zip；目前最新 zip 仍是 2026-03-30 的 `ecc-notes---1dfb6b33-12f4-42a3-985d-9fb1060d8ff6.zip`。聊天中目前可見的是一張投影片照片，不是待辦專案壓縮檔，因此尚未取得可解包的 source。

## 收到 Todo API 工具 tar，但仍缺實際站點設定
- source: chat + attachment + docs
- tags: #todo #plugin #openclaw #deployment
- note: 使用者後續改提供 `todo-api-tools---ba8b12ea-e393-4f9c-a32a-04f4e384cf19.tar` 作為 OpenClaw 存取待辦網站的小工具。從附件內容可判斷這是一個 `todo-api-tools` 插件，會註冊 list/create/update/delete/restore task 等工具，且需要至少 `baseUrl` 與 `apiToken` 兩個設定值。下一步應在取得實際站點 URL 與 token 後，安裝插件、寫入 config、重啟 gateway，再做一次工具驗證。
# 2026-04-03

## 記帳免批准流程實測成功
- source: chat + expense csv + reminder send
- tags: #expense #telegram #exec #approvals #verification
- note: 4/3 午餐「拉亞漢堡 206」已用新的固定流程完成：寫入 `expenses/2026-04.csv`、透過 `skills/expense-tracker/scripts/send-reminder.sh` 成功送出 reminder bot，且整體過程未再出現新的 approval prompt，表示 4/2 做的記帳 approval 降噪修正（allow-always + wrapper 腳本）已在真實單筆記帳上驗證成功。

## 待辦事項網頁專案：GitHub 內容不完整，暫時無法直接部署
- source: chat + github api/web fetch
- tags: #todo #github #deployment #audit
- note: 檢查 `yuming920619-svg/Myclawagi` 的 `待辦事項/` 後，確認目前只有 Next.js 骨架根目錄檔案，缺少 README 提到的 `app/`、`lib/`、`supabase/`、`docs/`、`tests/` 等關鍵內容。`middleware.ts` 匯入了不存在的 `@/lib/env`，`package.json` 也引用不存在的 `tests/task-schemas.test.ts`，因此目前不是單純少部署，而是 repo 尚未完整 push，需先補齊原始碼或由我接手重建後才能繼續部署。

## 待辦事項專案 zip 未成功傳入
- source: chat + exec + visible attachment
- tags: #todo #telegram #attachment #blocker
- note: 使用者嘗試直接上傳完整專案 zip，但檢查 `/home/node/.openclaw/media/inbound` 後，沒有出現 2026-04-03 的新 zip；目前最新 zip 仍是 2026-03-30 的 `ecc-notes---1dfb6b33-12f4-42a3-985d-9fb1060d8ff6.zip`。聊天中目前可見的是一張投影片照片，不是待辦專案壓縮檔，因此尚未取得可解包的 source。

## 收到 Todo API 工具 tar，但仍缺實際站點設定
- source: chat + attachment + docs
- tags: #todo #plugin #openclaw #deployment
- note: 使用者後續改提供 `todo-api-tools---ba8b12ea-e393-4f9c-a32a-04f4e384cf19.tar` 作為 OpenClaw 存取待辦網站的小工具。從附件內容可判斷這是一個 `todo-api-tools` 插件，會註冊 list/create/update/delete/restore task 等工具，且需要至少 `baseUrl` 與 `apiToken` 兩個設定值。下一步應在取得實際站點 URL 與 token 後，安裝插件、寫入 config、重啟 gateway，再做一次工具驗證。

## Todo 網站已自行部署到 Vercel，待接入 OpenClaw
- source: chat
- tags: #todo #plugin #vercel #integration
- note: 使用者已自行部署待辦網站，對外網址為 `https://mytodoapp-sable.vercel.app`。聊天中也已提供 OpenClaw 接入所需的 API token，但基於安全不寫入記憶檔。當前狀態只差使用者批准後執行插件安裝、寫入 `plugins.entries.todo-api-tools.config`、重啟 gateway，並驗證工具是否可正常列出待辦。

## Todo API 工具已接線完成，待新 session 實測
- source: chat + config edit + exec
- tags: #todo #plugin #gateway #verification
- note: 今日接線過程中發現 `todo-api-tools` 插件其實早已存在於 `/home/node/.openclaw/extensions/todo-api-tools`，不需重裝；真正完成的步驟是把 `baseUrl`/`apiToken` 寫入 `~/.openclaw/openclaw.json`，並成功執行 `openclaw gateway restart` + `openclaw status`（code 0）。接下來最穩妥的驗證方式是用新 session / 新回合直接要求「列出待辦」，確認 session 是否已刷新到新的工具清單。

## 使用者要求記住 Todo 小工具
- source: chat
- tags: #todo #plugin #memory
- note: 使用者明確要求把新加入的 `todo-api-tools` 小工具記住。之後若提到待辦管理，應優先想到這組工具，能力包含列出、新增、更新、刪除、還原待辦；不記錄 API token 等敏感資訊。

## Todo plugin 載入問題已定位並修正
- source: chat + config edit + openclaw status/plugins inspect
- tags: #todo #plugin #openclaw #fix #verification
- note: `todo-api-tools` 原本沒進工具清單，不是單純因為 `tools.allow`，真正根因是 `plugins.allow` 未設定，加上 `/home/node/.openclaw/extensions/todo-api-tools/index.js` 權限過寬（world-writable / mode 666）導致 plugin candidate 被 block。已補上 `plugins.allow`（並順手保留 `tools.allow`）、執行 `chmod -R go-w /home/node/.openclaw/extensions/todo-api-tools` 後，`openclaw status` 的 critical plugin block 警告已消失，`openclaw plugins inspect todo-api-tools` / `plugins list` 也能看到此 plugin（v0.1.0, Tools）。下一步是用新 session 實測原生 todo tools 是否直接出現在工具清單、是否可免 approval 操作。

## Todo API list / create 已完成端到端驗證
- source: chat + approved exec/api call
- tags: #todo #plugin #verification #api
- note: 在新 session 已成功列出 3 筆既有待辦，之後又成功新增一筆測試待辦 `測試`（HTTP 201）。表示目前至少 list / create 路徑已正常可用；若之後要補完驗證，可再測 update / delete / restore。
