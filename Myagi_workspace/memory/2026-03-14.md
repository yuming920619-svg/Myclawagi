# 2026-03-14

## 09:39 UTC — 工作流決定：中整合 + coding agent 評估
- source: chat
- tags: #workflow #proactive-agent #coding #verilog
- note: $今天確認 workspace 採 proactive-agent 的中整合模式：保留 SESSION-STATE.md、notes/open-loops.md、notes/areas/recurring-patterns.md 與輕量 heartbeat 維護；不啟用 full WAL、memory/working-buffer.md、full proactive tracking。\n另外已為 self-improving-agent 與 pitfall-loop 建立去重規則：預設先記 .learnings/*，只有明確是可重複踩到且具 rootCause/prevention、並經使用者同意的事項才進 pitfall-loop。\n關於寫 code（尤其 Verilog / RTL）工作流的評估：目前主 agent workflow 仍可用於 coding，但若 Verilog/RTL 成為高頻核心工作，建議之後考慮拆出一個輕量 code/rtl agent；目前尚未做最終決定。


## 10:09 UTC — 建立 token-guard 技能
- source: chat
- tags: #skills #token #quota #heartbeat
- note: $使用者決定實作專門檢查剩餘 token/quota 與提醒的 skill。已在 workspace 建立 `skills/token-guard/SKILL.md`，定位為輕量 quota 監控技能：透過 `session_status` 讀取 5 小時與週額度剩餘比例，分成 🟢/🟡/🟠/🔴 四級，並依使用者既有策略提供建議（Codex 主力、額度緊時切 Claude、優先減少工具回合與上下文長度）。同時已把 heartbeat 接上 quota 檢查，但只在 🟠 Warning 或 🔴 Critical 時主動提醒，避免平常過度打擾。

## 10:33 UTC — 更新模型容災規則為方案 B
- source: chat
- tags: #models #failover #quota
- note: $使用者確認目前模型容災採用品質優先的方案 B。現行規則為：primary `openai-codex/gpt-5.4`；fallback 依序為 `anthropic/claude-opus-4-6` → `anthropic/claude-sonnet-4-6` → `anthropic/claude-sonnet-4-5`。之後討論 failover、切模型、quota 用盡時的降落行為時，應以這組規則為準。

## 11:00 UTC — OpenClaw 2026.3.13 vs 2026.3.8 / Zeabur 升級風險整理
- source: chat
- tags: #openclaw #release #zeabur #deployment
- note: $整理 OpenClaw 2026.3.13 相對 2026.3.8 的差異與社群回報。結論：3.13 整體能力與安全性優於 3.8，但 3.12/3.13 的 Control UI / pairing / reverse proxy / Docker 部署面仍有真實使用者回報。對 Zeabur 特別要注意 `gateway.controlUi.allowedOrigins`、不要使用 `latest` 而要 pin 明確 image tag、以及小記憶體主機可能 OOM。若要升級，建議先在 staging 驗證 Control UI、pairing、WebSocket、cron 與訊息通道。

## 11:20 UTC — 天堂W 首領通知檢查：cron 正常、LINE 端 hit 429
- source: chat
- tags: #openclaw #cron #line #天堂W
- note: $檢查天堂W首領通知任務時，確認排程器本身正常觸發，今天前段通知（如蜘蛛）有成功送達；但後續多個 LINE 任務出現 `HTTPFetchError: 429 - Too Many Requests`。判斷較像 LINE 發送階段 rate limit，而不是使用者當時在 Telegram 跟我對話造成的模型或 cron 故障。

## 11:55 UTC — Claude 付款問題：台灣支援，但郵局 Visa 金融卡可能被銀行/3DS 擋
- source: chat
- tags: #claude #billing #taiwan #visa #debit
- note: $查詢 Anthropic 官方付款與 declined 說明後，確認台灣在 Claude.ai 與 API 的 supported countries 清單內，且官方接受 credit card 與 debit card。使用者提到自己使用台灣郵局 Visa 卡付款一直被拒；目前判斷較可能是發卡銀行風控、3D Secure 驗證或 billing address 不一致，而不是台灣地區本身不支援。後續若再談這題，優先建議檢查國外網購/3DS/定期扣款是否開啟，以及 billing address 是否與銀行留存完全一致。

## 13:30 UTC — Anthropic 認證配置已變更：移除 manual，僅保留 ryan
- source: chat
- tags: #anthropic #auth #openclaw #models
- note: $今天確認同一個 OpenClaw main agent 原先同時存在兩組 Anthropic setup-token profile：`anthropic:manual` 與 `anthropic:ryan`。之後依使用者要求，已移除 `anthropic:manual`，目前只保留 `anthropic:ryan`。因此後續新 session 預設也會直接使用 `anthropic:ryan`，不再回切到 `manual`。

## 15:55 UTC — OpenClaw 已升級至 2026.3.11，cron 驗證結果：Telegram explicit 正常，LINE explicit 仍遇 429
- source: chat
- tags: #openclaw #upgrade #cron #telegram #line
- note: $使用者已將 OpenClaw 從 2026.3.8 升級至 2026.3.11，並執行 `openclaw doctor --fix`。我確認 gateway 版本已是 2026.3.11，cron jobs 仍有 63 個，未因升級遺失；但 store 仍混有 legacy 痕跡（36 個 `notify:true`、24 個 announce-only 無 explicit target）。之後手動驗證 3 個代表性 cron：1) 現有 LINE explicit job（蜘蛛提醒）執行後仍回 `HTTPFetchError: 429 - Too Many Requests`，像是通道 rate limit / 發送問題；2) 臨時 Telegram explicit one-shot job 成功 delivered；3) 臨時 announce-only last-route 測試 job 失敗且為 429，疑似 last-route 解析到受限通道或該路徑本身仍有問題。臨時測試 job 已清理，避免之後重送。

## 16:02 UTC — announce-only 舊 cron jobs 已批次改成 Telegram explicit target
- source: chat
- tags: #openclaw #cron #migration #telegram
- note: $依使用者要求，已將 jobs.json 中所有 announce-only（`delivery.mode=announce` 但缺少 `channel` / `to`）的 cron jobs 批次改成 explicit Telegram 目標：`channel=telegram`、`to=8087282597`。本次共更新 24 個 job，包含舊的天氣提醒、天堂W Telegram 舊提醒，以及目前啟用中的「每週自我迭代回顧（踩坑系統）」。更新後 announce-only 數量已降為 0；目前共有 25 個 explicit Telegram announce jobs。
## 16:32 UTC — 洗澡後 1 小時提醒已建立
- source: chat
- tags: #reminder #telegram #cron
- note: 使用者表示先去洗澡，大約 1 小時後若仍未回訊要提醒。已建立一次性 cron job `97c3d074-9c35-473e-ab8e-ee4dc407ea39`，預計於 2026-03-14 17:30 UTC 執行，目標為 Telegram 直接訊息；job 內容會先檢查 session `agent:main:telegram:direct:8087282597` 在 2026-03-14T16:30:00Z 之後是否已有新的使用者訊息，若有則輸出 NO_REPLY，否則送出簡短提醒。

## 17:54 UTC — 所有 cron 通知已切到 Telegram reminder bot
- source: chat
- tags: #cron #telegram #reminder #migration
- note: 依使用者要求，已將所有帶有 announce delivery 的 cron jobs（共 59 個）統一改成走 Telegram reminder bot：`delivery.channel=telegram`、`delivery.to=8087282597`、`delivery.accountId=reminder`。這包含原本已在 Telegram 的提醒，以及先前仍走 LINE 的天氣、天堂W、愛麗莎惡夢箱、文獻晨報等通知型 jobs。之後驗證 `每日 07:30 台北天氣提醒（原 LINE 版）` 手動執行成功，`status=ok`、`delivered=true`，代表 reminder bot 路由可正常送達。

## 17:55 UTC — OpenClaw 升級後的 cron 分流工作流
- source: chat
- tags: #openclaw #cron #workflow #telegram
- note: 使用者要求記住一個升級後處理流程：OpenClaw 升級後 → cron 盤點 → 通知測試 → 多 bot 分流。這可作為之後版本升級或通知系統調整時的標準排查/遷移順序。

