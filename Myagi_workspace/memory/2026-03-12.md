# 2026-03-12

## OpenClaw / Browser sandbox
- 已在 Zeabur 上把 OpenClaw 接上 remote browser sandbox；可用的 service 名稱是 `openclaw-sandbox-browser-posess`。
- `openclaw.json` 的 browser remote profile 已驗證可用；之後可直接用 remote profile 操作網站、截圖與快照。
- 這次排查的關鍵坑：一開始用錯 service 名稱（`openclaw-sandbox-browser`），後來改成 `openclaw-sandbox-browser-posess` 才成功連上。

## Skills 安裝與整合
- 已安裝 shared skill：`~/.openclaw/skills/self-improving-agent`。
- 已完成 self-improving-agent 的最小啟動版：建立 `workspace/.learnings/`（`LEARNINGS.md`、`ERRORS.md`、`FEATURE_REQUESTS.md`），並在 `AGENTS.md` 加入輕量 self-improvement workflow；未啟用 hook。
- 已安裝 shared skill：`~/.openclaw/skills/proactive-agent`。
- proactive-agent 採安全整合：只吸收低風險 guardrails（提示注入防護、安裝前先看 skill/scripts、先驗證再報完成、輕量 resourcefulness、heartbeat 的安全異常提醒），沒有覆蓋現有 `SOUL.md` / `USER.md` / `MEMORY.md`，也沒有啟用它較重的 WAL / working-buffer 流程。

## 自動任務調整
- 已暫時停用每日 MRAM ECC 文獻晨報（jobId `bc7a8931-0662-49ff-812a-7bf83811356c`）。
- 暫停原因：使用者認為在沒有穩定全文權限的情況下，每日只整理摘要的效益不高。
- 後續若恢復，較值得改成：每週一次、主題式追蹤、或只推真正值得精讀的少數 paper。

## 安裝方式與經驗
- ClawHub CLI 安裝 `self-improving-agent` 時遇到 rate limit；改用手動方式安裝成功。
- fresh session 已驗證看得到 `self-improvement` 與 `proactive-agent` 這兩個 shared skills。
