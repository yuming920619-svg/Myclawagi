# 2026-04-09

## 天堂W 首領 / 活動提醒已全數停用
- source: chat + cron audit/edit
- tags: #cron #game #telegram #cleanup
- note: 使用者表示目前已不玩天堂W，要求關掉所有相關通知。我先用 `openclaw cron list` 篩出所有名稱含 `天堂W` 的 jobs，確認包含 BOSS 與活動提醒共 22 筆，之後逐筆以 `openclaw cron edit <id> --disable` 停用。後續 `openclaw cron list | grep 天堂W` 已無結果，表示目前已無啟用中的天堂W相關 cron。

## OpenClaw cron disable CLI 參數坑
- source: exec failure + retry
- tags: #openclaw #cron #cli #gotcha
- note: `openclaw cron disable <id>` 在目前版本（2026.4.1）實測回 `invalid cron.update params`，看起來 CLI / Gateway 對 `id` vs `jobId` 參數有落差；改用 `openclaw cron edit <id> --disable` 可正常停用。
